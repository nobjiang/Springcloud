server:
  port: 80
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka/
ribbon:
  ConnectTimeout: 5000
  ReadTimeout: 5000

logging:
  level:
    com.consumer80.feign.FeignClients.PaymentFeignService: debug
